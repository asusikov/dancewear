%h2
	= @item.title
.row-fluid.dotted
	.span5
		= image_tag @item.avatar.url(:full)
	.span7.good-description
		.row-fluid
			%h3
				= number_to_currency @item.price, :precision => 0
		.row-fluid.dotted
			= @item.description.html_safe